# Widget picker preview images

Drop a PNG per variant here, named `<type>-<variant>.png`. The picker shows it
on that variant's card; if absent it falls back to the app icon.

Expected files (dark-mode screenshots of the widget itself, cropped tight):

  clock-digital.png     clock-analog.png     clock-classic.png    clock-dial.png
  cityclock-world.png   cityclock-worldRow.png
  weather-conditions.png  weather-forecast.png
  calendar-month.png    calendar-agenda.png
