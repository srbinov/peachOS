export function logInfo(text) {
    console.log("PerfectLockScreen: " + text);
}

export function logWarn(text) {
    console.warn("PerfectLockScreen: " + text);
}

export function logError(text) {
    console.error("PerfectLockScreen: " + text);
}